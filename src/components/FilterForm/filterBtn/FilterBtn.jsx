const FilterBtn = () => {
    return (
        <div className="filter__buttons">
            <button className="filter__show">Показать объекты</button>
            <button className="filter__reset" type="reset">Сбросить фильтр</button>
        </div>
    )
}

export default FilterBtn